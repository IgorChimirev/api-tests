#!/bin/sh

# Показываем ЧЕРЕМШУ на весь экран с громким звуком
python3 "$(dirname "$0")/fullscreen.py" &

# Запускаем тесты
echo "Запускаем тесты перед пушем..."
go test ./...

if [ $? -ne 0 ]; then
  echo "Тесты упали. Пуш отменён."
  exit 1
fi

echo "Все тесты прошли. Пуш продолжается. 🚀"
